#!/bin/bash
for exe in "$@"; do
  exe_path=$(which "${exe}" 2>/dev/null)
  if [[ ! -x "${exe_path}" ]]; then
    errors="${errors}\nplease install ${exe}"
  fi
done
if [[ -n "${errors}" ]]; then
  printf "ERROR: prerequisites missing!${errors}\n" 1>&2
  exit 2
fi
